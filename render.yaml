services:
  - type: web
    name: eneba-hw
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: eneba-hw-db
          property: connectionString

databases:
  - name: eneba-hw-db
    plan: free
